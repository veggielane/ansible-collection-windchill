# Line endings: LF in the repository and on checkout (the roles run inside a
# Linux control node), except Windows scripts, which want CRLF.
* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
